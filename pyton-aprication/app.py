"""AIライティングツール ホーム画面。"""
import streamlit as st

from lib.ui import check_api_key

st.set_page_config(page_title="AIライティングツール", page_icon="✍️", layout="wide")

st.title("✍️ AIライティングツール")
st.caption("Gemini APIを使った個人用ライティング支援アプリ")

check_api_key()

st.markdown(
    """
左のメニューから使いたい機能を選んでください。

### 機能一覧
| 機能 | 説明 |
| --- | --- |
| 📝 ブログ記事ライター | テーマとキーワードからブログ記事を執筆します |
| 📧 メール返信文 | 受信メールの内容から返信文の下書きを作ります |
| 📄 文章要約 | 長い文章を指定した長さに要約します |
| ✨ 校正・リライト | 文章の誤字脱字修正やトーン変更を行います |
| 🏷️ タイトル・説明文・タグ生成 | 動画やブログ用にタイトル案・説明文・ハッシュタグ・タグをまとめて生成します |
| 🖼️ サムネイル画像生成 | Gemini画像生成モデルでサムネイル画像を作成します |
| 🎬 台本作成 | 動画・音声コンテンツ用の台本を作成します |
| 💡 キャッチコピー生成 | 見出しやキャッチコピーの候補を複数生成します |

### 使い方
1. プロジェクト直下の `.env` ファイルに `GEMINI_API_KEY` を設定
2. ターミナルで `streamlit run app.py` を実行
3. 左のメニューから機能を選んで利用
"""
)
